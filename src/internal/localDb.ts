/**
 * @internal
 * Test-only re-exports for `src/services/storage/localDb.ts`.
 * These symbols are intentionally not part of the public API surface.
 * Import only from test files — never from production code.
 */
export { LocalDatabaseService } from '../services/storage/localDb';
